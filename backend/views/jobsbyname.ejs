<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="container mx-auto my-6">
        <% jobs.forEach((job) => { %>
          <div class="max-w-md ml-5 my-4 p-8 bg-blue-900 text-white rounded-lg shadow-lg">
             <div class="flex justify-between items-center">
              <h2 class="text-xl font-bold mb-2"><%= job.title %></h2>
              <img class="w-10 ml-50" src="../images/placeholder.png">
          </div>
            <h3 class="text-lg font-semibold mb-2">Company: <%= job.company %></h3>
            <p class="text-sm mb-4"><%= job.description %></p>
            <div class="flex justify-between items-center">
              <span class="text-sm font-medium">Location: <%= job.location %></span>
              <a 
                href="/application/apply/<%= job._id %>" 
                class="bg-[#7CF5FF] hover:bg-blue-200 text-black font-bold py-2 px-4 rounded"
                target="_blank"
              >
                Apply Now
              </a>
            </div>
          </div>
        <% }); %>
      </div>
      
      
    
</body>
</html>